# 更新日志

## v1.2.0 (2026-02-02) - 晋级率计算修复

### 修复的问题

#### 晋级率计算修复 ⚠️ **重要**

**问题**：增量更新后，2026-02-02 及之后的晋级率全部显示为 0%

**原因**：
- 计算晋级率需要同时用到前一日的 `limit_data`（涨停数据）和 `daily`（日线数据）
- 增量更新时，`prev_data` 只包含了 `limit_data`，缺少 `daily` 字段
- 导致无法正确获取"今日有交易"的股票列表，分母计算错误

**修复内容**：
1. **修改 `indicators.py`**：优化晋级率分母口径
   - 分母 = 昨日 N 板**且今日有交易**的只数（排除今日停牌的股票）
   - 避免停牌股票拉低晋级率

2. **修改 `update_data.py`**：增量更新时同时加载前一日的 `limit_data` 和 `daily`
   ```python
   # 修改前
   prev_data = {'limit_data': prev_limit}
   
   # 修改后
   prev_data = {
       'limit_data': prev_limit_df,
       'daily': prev_daily_df
   }
   ```

3. **新增 `storage.py`**：添加 `load_daily_data()` 方法，支持读取指定日期的日线数据

4. **重新计算**：重新计算了所有历史数据的晋级率

**修复效果**（以 2026-02-02 为例）：
- 修复前：1进2 = 0%, 2进3 = 0%, 3进4 = 0%
- 修复后：1进2 = 17.07%, 2进3 = 60.0%, 3进4 = 0%（正确）

**影响范围**：所有历史数据已重新计算

---

## v1.1.0 (2026-01-27) - 数据获取和计算逻辑重大修正

### 🔧 修复的问题

#### 1. 数据获取优化
- **修改前**：分3次调用API获取涨停(U)、跌停(D)、炸板(Z)数据
- **修改后**：一次性获取所有涨跌停数据，通过`limit`字段区分类型
- **新增字段**：`limit_amount`, `float_mv`, `total_mv`, `turnover_ratio`, `first_time`, `last_time`
- **优势**：减少API调用次数，获取更完整的数据

#### 2. 红盘率定义修正 ⚠️
- **修改前**：`close >= open` 的比例（不准确）
- **修改后**：`open >= pre_close` 的比例
- **说明**：红盘指开盘价高于前收盘价，更符合市场习惯

#### 3. 晋级率计算修正 ⚠️
- **修改前**：昨日N板 → 今日N+1板（定义错误）
  - 例如：1进2 = 昨日首板且今日二板的比例
- **修改后**：昨日N板 → 今日任意涨停
  - 例如：1进2 = 昨日首板，今日再次涨停的比例（不限几板）
- **说明**：符合"晋级"的真实含义 - 继续涨停即为晋级

#### 4. 新增"昨日涨停今日表现"指标
新增6个指标，追踪昨日涨停股票今日的表现：
- `yesterday_first_premium` - 昨日首板今日平均涨幅
- `yesterday_first_red_rate` - 昨日首板今日红盘率
- `yesterday_second_premium` - 昨日二板今日平均涨幅
- `yesterday_second_red_rate` - 昨日二板今日红盘率
- `yesterday_third_premium` - 昨日三板以上今日平均涨幅
- `yesterday_third_red_rate` - 昨日三板以上今日红盘率

### 📊 指标计算说明

#### 当日涨停板指标
- `first_red_rate` - 今日首板开盘红盘率
- `first_premium` - 今日首板平均涨幅
- `second_red_rate` - 今日二板开盘红盘率
- `second_premium` - 今日二板平均涨幅

#### 昨日涨停今日表现
- `yesterday_first_premium` - 昨日首板今日平均涨幅（溢价）
- `yesterday_first_red_rate` - 昨日首板今日开盘红盘率

### 🔄 数据迁移建议

由于计算逻辑重大修改，建议：

1. **删除旧数据重新初始化**：
```bash
del data\processed\emotion_cycle.parquet
del data\raw\limit_list.parquet
python update_data.py --start 20260105 --end 20260127
```

2. **或者补充新数据**：
```bash
python update_data.py --start 20260105 --end 20260127
```

### ⚠️ 重要提示

- 新旧数据的指标含义已改变，不建议直接对比
- 晋级率现在更准确反映"连板续涨"的能力
- 红盘率改为开盘红盘率，更有参考价值

---

## v1.0.0 (2026-01-27)

初始版本发布
